/**
 * Stub for the `server-only` guard.
 *
 * That package throws when imported outside a server component, which would
 * stop Vitest from loading server modules at all. The guard protects the
 * browser bundle, not the test runner, so it is aliased away here.
 */
export {};
